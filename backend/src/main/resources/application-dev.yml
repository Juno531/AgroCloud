spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

logging:
  level:
    com.farm.erp: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
